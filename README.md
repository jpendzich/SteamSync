# Build Instructions
- Place https://github.com/mtkennerly/ludusavi-manifest/blob/master/data/manifest.yaml or other manifest of same format into `content/manifest` folder
- Run `build.sh`
- Run `build/SteamSync`
